<div id="taskWrapper">
	<%= form_for(@task) do |f| %>
	  <% if @task.errors.any? %>
		<div id="error_explanation">
		  <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

		  <ul>
		  <% @task.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		  <% end %>
		  </ul>
		</div>
	  <% end %>
	 
	  <div class="field">
		<%= f.label :name %>
		<%= f.text_field :name %>
	  </div>
	  <div class="field">
		<%= f.label :completion_date %>
		<%= f.date_select :completion_date %>
	  </div>
	  <div class="field">
		<%= f.label :Completed %>
		<%= f.check_box :date_completed %>
	  </div>
	  <div class="actions">
		<%= f.submit %>
	  </div>

<% end %>
</div>